/**
 * visszaadja a belejelentkezett felhasználó mai étkezéseit
 * @param objRepo 
 * @returns {function(*, *, *): *}
 */
module.exports = (objRepo) => {
    return (req, res) => {
        return next();
    }
};